Commit the current in-flight work on `main` as a single clean commit (matching the repo's existing commit style, e.g. `cfa49dd`).

**What's being committed** (from git status):
- New: `client/app/(store)/locations/page.tsx` — the `/locations` route
- New: `client/app/components/store/locations-page.tsx` — full locations page (selectable cards synced to Leaflet map)
- Modified: `client/app/(store)/layout.tsx`, `client/app/page.tsx`, `client/app/globals.css`
- Modified: `client/app/components/store/menu-page.tsx`, `client/app/components/store/download-app.tsx`
- Modified: `client/app/components/admin/ui/dropdown.tsx` (import path fix `@/components/...` → `@/app/components/...`)
- Modified: `client/pnpm-lock.yaml`

**Steps**:
1. `git add -A` from the repo root (`C:/Users/husna/Desktop/crispy`) to stage the 7 modified files + 2 untracked paths
2. `git commit` with message:
   `feat: add standalone /locations page with interactive map, refine menu page and store layout, fix admin dropdown import`
3. Verify with `git status --short` and `git log --oneline -1`
4. Do **not** push — local commit only

**Permissions needed**: run git add/commit/status/log in the repository.